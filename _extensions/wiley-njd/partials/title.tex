$if(title)$
\title{$title$}
$endif$

$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$]{$by-author.name.literal$}
$endfor$

\authormark{$author-mark$}
$if(title)$
\titlemark{$title-mark$}
$endif$

$for(by-affiliation)$
\address[$it.number$]{%
  \orgdiv{%
    $if(it.department)$$it.department$$endif$%
    $if(it.department)$, $endif$%
    \orgname{%
      $if(it.name)$$it.name$$endif$%
      $if(it.name)$, $endif$%
    }%
    \orgaddress{%
      \state{$if(it.city)$$it.city$$endif$}%
      $if(it.city)$, $endif$%
      \country{$if(it.country)$$it.country$$endif$}%
    }%
  }%
}
$endfor$

$-- Correspondence
$for(by-author)$
$if(by-author.attributes.corresponding)$
\corres{
$if(corres-line)$
$corres-line$
$else$
$by-author.name.literal$
$endif$
\email{$by-author.email$}}$endif$
$endfor$
$if(present-address)$
\presentaddress{$present-address$}
$endif$

%\fundingInfo{Text}
%\JELinfo{ejlje}

$-- Abstract
\abstract[Abstract]{$abstract$}

$-- Keywords
$if(keywords)$
\keywords{$for(keywords)$$it$$sep$, $endfor$}
$endif$

$-- JNL citation
\jnlcitation{\cname{%
$for(by-author)$\author{$by-author.name.literal$}$sep$, $endfor$}.
\ctitle{$title$.} \cjournal{\it $journal$} \cvol{$year$;$volume$($issue$):$pages$}.}

\maketitle

$if(abbreviations-note)$
\renewcommand\thefootnote{}
\footnotetext{\textbf{Abbreviations:} $abbreviations-note$}

\renewcommand\thefootnote{\fnsymbol{footnote}}
\setcounter{footnote}{1}
$endif$