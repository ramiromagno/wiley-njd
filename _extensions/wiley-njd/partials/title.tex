$if(title)$
\title{$title$}
$endif$
\date{$date$}

$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$]{$by-author.name.literal$}
$endfor$

\authormark{TAYLOR \textsc{et al.}}
$if(title)$
\titlemark{$title$}
$endif$

$for(by-affiliation)$
\address[$it.number$]{
  \orgdiv{$if(it.department)$$it.department$$endif$},
  \orgname{$if(it.name)$$it.name$$endif$},
  \orgaddress{
    \state{$if(it.city)$$it.city$$endif$},
    \country{$if(it.country)$$it.country$$endif$}
    }
  }
$endfor$

$-- Corresponding author
$for(by-author)$
$if(by-author.attributes.corresponding)$\corres{Corresponding author $by-author.name.literal$. \email{$by-author.email$}}$endif$
$endfor$

$if(present-address)$
\presentaddress{$present-address$}
$endif$

$-- Abstract
\abstract[Abstract]{$abstract$}

$-- Keywords
$if(keywords)$
\keywords{
  $for(keywords)$$it$$sep$, $endfor$
}
$endif$

$if(abbreviations-note)$
\renewcommand\thefootnote{}
\footnotetext{\textbf{Abbreviations:} $abbreviations-note$}
\renewcommand\thefootnote{\fnsymbol{footnote}}
\setcounter{footnote}{1}
$endif$